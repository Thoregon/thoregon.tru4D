/**
 * triggers a command (sequence) by a timer
 *
 * @author: Bernhard Lukassen
 */

export default class TimerTrigger {

    constructor({
                    id
                } = {}) {
        Object.assign(this, {id});
    }

}
